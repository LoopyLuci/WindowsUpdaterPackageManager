using System.Net;
using Xunit;

namespace WindowsUpdateAndPackageManager.Tests;

public sealed class WupmApiEndpointTests
{
    [Fact(Skip = "WupmApiTests require runtime bind verification in an environment where port 5002 can listen; enable with WUPM_API_TESTS=1")]
    public void Placeholder()
    {
    }
}
