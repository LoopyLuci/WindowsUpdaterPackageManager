<UserControl x:Class="WupmGui.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel Margin="16">
    <TextBlock Text="History" FontSize="24" FontWeight="Bold" DockPanel.Dock="Top" Margin="0,0,0,12"/>
    <DataGrid AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp}" Width="180"/>
        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="150"/>
        <DataGridTextColumn Header="Package" Binding="{Binding PackageId}" Width="*"/>
        <DataGridTextColumn Header="Success" Binding="{Binding Success}" Width="100"/>
      </DataGrid.Columns>
    </DataGrid>
  </DockPanel>
</UserControl>
